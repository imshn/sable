services:
  - type: web
    name: sable-relay
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install --omit=dev
    startCommand: npx tsx server/index.ts
    healthCheckPath: /healthz
